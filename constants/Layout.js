import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("screen");
                        // Dimensions 디바이스의 height, width 구해줌.
// 오브젝트 생성. 
export default {
    width,
    height
};